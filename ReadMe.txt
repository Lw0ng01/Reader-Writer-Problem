My code starts by solving the problems by creating 3 different semaphores, for Mutex, and 
one for keeping track of the read and write functions. There is also separate variables for keeping track of
waiting and active readers/writers. It solves the problem in that writers always have priority.
